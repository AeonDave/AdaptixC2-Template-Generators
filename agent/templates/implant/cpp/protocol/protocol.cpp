// __NAME__ Agent — Wire Protocol Implementation
//
// Protocol framing, serialization, and parsing utilities.
// This file can be replaced by the protocol generator.

#include "protocol.h"

// TODO: Implement protocol packing/unpacking utilities
//
// Typical pattern:
//   PacketHeader* CreatePacket(BYTE type, BYTE* data, ULONG size);
//   BOOL ParsePacket(BYTE* raw, ULONG rawSize, PacketHeader** header, BYTE** payload);
